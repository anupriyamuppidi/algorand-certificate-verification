def store_certificate(student_name, certificate_id):
    print("Storing certificate on Algorand blockchain...")
    print(f"Student: {student_name}, Certificate ID: {certificate_id}")

def verify_certificate(certificate_id):
    print(f"Verifying certificate ID: {certificate_id}")
